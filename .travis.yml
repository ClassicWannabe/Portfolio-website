language: python
python:
  - "3.9"

install: pip install -r requirements.txt

jobs:
  include:
    - name: "Linter"
      before_script:
        - pip install flake8
      script:
        - flake8

    - name: "Django Test"
      script:
        - pytest
